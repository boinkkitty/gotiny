syntax = "proto3";

package redirect;

option go_package = "gotiny/proto/redirect";

service RedirectService {
  rpc Resolve(ResolveRequest) returns (ResolveResponse);
}

message ResolveRequest {
  string short_code = 1;
}

message ResolveResponse {
  string original_url = 1;
}
